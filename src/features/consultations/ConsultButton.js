import { Button } from 'reactstrap'

const ConsultButton = () => {
    const handleClick = () => {

    }

    return (
        <Button onClick={handleClick}>
            Free Consultation
        </Button>
    )
}